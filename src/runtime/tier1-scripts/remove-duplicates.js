export const script = "function showToast(m){const t=document.getElementById('toast');t.textContent=m||'Copied to clipboard!';t.classList.add('show');setTimeout(()=>t.classList.remove('show'),1500);}\nfunction copyText(id){const el=document.getElementById(id);navigator.clipboard.writeText(el.value||el.textContent).then(()=>showToast());}\nfunction copyOutput(id){const el=document.getElementById(id);navigator.clipboard.writeText(el.textContent).then(()=>showToast());}\n\nfunction dedup(){const el=document.getElementById('t-input');const lines=el.value.split('\\n').filter(l=>l.trim());const u=[...new Set(lines)];el.value=u.join('\\n');document.getElementById('t-output').textContent=lines.length+' lines → '+u.length+' unique ('+(lines.length-u.length)+' removed)';}\nfunction sortL(){const el=document.getElementById('t-input');el.value=el.value.split('\\n').filter(l=>l.trim()).sort().join('\\n');}\nconst ads=[\"Custom Print on Demand Apparel — Free Storefront for Your Business\",\"Sell Custom Apparel — We Handle Printing & Free Shipping\",\"Launch Your Own Clothing Brand — No Inventory, No Risk\"];let adIdx=Math.floor(Math.random()*ads.length);\nfunction rotateAds(){const el=document.getElementById('ad');if(el)el.textContent=ads[adIdx];adIdx=(adIdx+1)%ads.length;}rotateAds();setInterval(rotateAds,8000);";
