export const script = "\n// ── Utility ──\nfunction showToast(m){const t=document.getElementById('toast');t.textContent=m||'Done!';t.classList.add('show');setTimeout(()=>t.classList.remove('show'),1500);}\nfunction esc(s){return s.replace(/&/g,'&amp;').replace(/\"/g,'&quot;').replace(/</g,'&lt;').replace(/>/g,'&gt;');}\n\n// ── DOM refs ──\nconst titleInput=document.getElementById('pageTitle');\nconst descInput=document.getElementById('metaDesc');\nconst urlInput=document.getElementById('pageUrl');\nconst siteNameInput=document.getElementById('siteName');\nconst pubDateInput=document.getElementById('pubDate');\nconst breadcrumbInput=document.getElementById('breadcrumbPath');\nconst faviconFile=document.getElementById('faviconFile');\nconst faviconClear=document.getElementById('faviconClear');\nconst titleCount=document.getElementById('titleCount');\nconst descCount=document.getElementById('descCount');\nconst urlCount=document.getElementById('urlCount');\nconst serpBox=document.getElementById('serpBox');\nconst serpTitle=document.getElementById('serpTitle');\nconst serpDesc=document.getElementById('serpDesc');\nconst serpDate=document.getElementById('serpDate');\nconst serpUrl=document.getElementById('serpUrl');\nconst serpSiteName=document.getElementById('serpSiteName');\nconst serpFavicon=document.getElementById('serpFavicon');\nconst faviconPreview=document.getElementById('faviconPreview');\n\nlet isMobile=false;\nlet faviconDataUrl=null;\n\n// ── Character counters ──\nfunction updateTitleCount(){\n  const len=titleInput.value.length;\n  if(len===0){titleCount.textContent='0 characters \\u2014 aim for 50-60';titleCount.className='char-count green';}\n  else if(len>=50&&len<=60){titleCount.textContent=len+' characters \\u2014 perfect length';titleCount.className='char-count green';}\n  else if((len>=40&&len<50)||(len>60&&len<=65)){titleCount.textContent=len+' characters \\u2014 '+(len<50?'a bit short':'slightly long');titleCount.className='char-count yellow';}\n  else{titleCount.textContent=len+' characters \\u2014 '+(len<40?'too short, add more':'too long, will be truncated');titleCount.className='char-count red';}\n}\n\nfunction updateDescCount(){\n  const len=descInput.value.length;\n  if(len===0){descCount.textContent='0 characters \\u2014 aim for 120-160';descCount.className='char-count green';}\n  else if(len>=120&&len<=160){descCount.textContent=len+' characters \\u2014 perfect length';descCount.className='char-count green';}\n  else if((len>=100&&len<120)||(len>160&&len<=170)){descCount.textContent=len+' characters \\u2014 '+(len<120?'a bit short':'slightly long');descCount.className='char-count yellow';}\n  else{descCount.textContent=len+' characters \\u2014 '+(len<100?'too short':'too long, will be truncated');descCount.className='char-count red';}\n}\n\nfunction updateUrlCount(){\n  const len=urlInput.value.length;\n  urlCount.textContent=len+' characters';\n  urlCount.style.color=len>75?'var(--orange)':'var(--text-dim)';\n}\n\n// ── SERP preview update ──\nfunction updatePreview(){\n  // Title\n  let title=titleInput.value||'Your Page Title Will Appear Here';\n  const titleLimit=isMobile?55:60;\n  if(title.length>titleLimit){\n    serpTitle.textContent=title.substring(0,titleLimit).trim()+'...';\n  }else{\n    serpTitle.textContent=title;\n  }\n\n  // Description\n  let desc=descInput.value||'Your meta description will appear here. Google typically shows about 155-160 characters on desktop and slightly fewer on mobile. Write compelling copy that makes searchers want to click.';\n  const descLimit=isMobile?130:160;\n  let dateStr='';\n  if(pubDateInput.value){\n    const d=new Date(pubDateInput.value+'T00:00:00');\n    const months=['Jan','Feb','Mar','Apr','May','Jun','Jul','Aug','Sep','Oct','Nov','Dec'];\n    dateStr=months[d.getMonth()]+' '+d.getDate()+', '+d.getFullYear()+' \\u2014 ';\n  }\n  serpDate.textContent=dateStr;\n  if(dateStr) serpDate.className='serp-date'; else serpDate.className='';\n\n  if(desc.length>descLimit){\n    serpDesc.textContent=desc.substring(0,descLimit).trim()+'...';\n  }else{\n    serpDesc.textContent=desc;\n  }\n\n  // URL / breadcrumbs\n  let rawUrl=urlInput.value||'https://example.com';\n  // Display URL: strip protocol\n  let displayUrl=rawUrl.replace(/^https?:\\/\\//,'');\n  if(displayUrl.length>60) displayUrl=displayUrl.substring(0,57)+'...';\n\n  const breadcrumb=breadcrumbInput.value.trim();\n  if(breadcrumb){\n    const parts=breadcrumb.split('>').map(s=>s.trim()).filter(Boolean);\n    let domain=rawUrl.replace(/^https?:\\/\\//,'').split('/')[0]||'example.com';\n    serpUrl.innerHTML='<span class=\"serp-breadcrumb-url\">'+esc(domain)+' <span>&#8250;</span> '+parts.map(p=>esc(p)).join(' <span>&#8250;</span> ')+'</span>';\n  }else{\n    serpUrl.textContent=displayUrl;\n  }\n\n  // Site name\n  let sName=siteNameInput.value.trim();\n  if(!sName){\n    try{\n      const u=new URL(rawUrl.startsWith('http')?rawUrl:'https://'+rawUrl);\n      sName=u.hostname.replace(/^www\\./,'');\n    }catch(e){sName='example.com';}\n  }\n  serpSiteName.textContent=sName;\n\n  // Favicon\n  const globeSvg='<svg viewBox=\"0 0 24 24\" fill=\"#70757a\"><path d=\"M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm-1 17.93c-3.95-.49-7-3.85-7-7.93 0-.62.08-1.21.21-1.79L9 15v1c0 1.1.9 2 2 2v1.93zm6.9-2.54c-.26-.81-1-1.39-1.9-1.39h-1v-3c0-.55-.45-1-1-1H8v-2h2c.55 0 1-.45 1-1V7h2c1.1 0 2-.9 2-2v-.41c2.93 1.19 5 4.06 5 7.41 0 2.08-.8 3.97-2.1 5.39z\"/></svg>';\n  if(faviconDataUrl){\n    serpFavicon.innerHTML='<img src=\"'+faviconDataUrl+'\" alt=\"favicon\">';\n    faviconPreview.innerHTML='<img src=\"'+faviconDataUrl+'\" alt=\"favicon\" style=\"width:18px;height:18px;object-fit:contain;\">';\n  }else{\n    serpFavicon.innerHTML=globeSvg;\n    faviconPreview.innerHTML=globeSvg;\n  }\n}\n\n// ── Toggle desktop/mobile ──\nconst viewToggle=document.getElementById('viewToggle');\nconst lblDesktop=document.getElementById('lblDesktop');\nconst lblMobile=document.getElementById('lblMobile');\n\nviewToggle.addEventListener('click',function(){\n  isMobile=!isMobile;\n  viewToggle.classList.toggle('mobile',isMobile);\n  lblDesktop.classList.toggle('active',!isMobile);\n  lblMobile.classList.toggle('active',isMobile);\n  serpBox.classList.toggle('desktop',!isMobile);\n  serpBox.classList.toggle('mobile',isMobile);\n  // Adjust font sizes for mobile\n  if(isMobile){\n    serpTitle.style.fontSize='18px';\n  }else{\n    serpTitle.style.fontSize='20px';\n  }\n  updatePreview();\n});\n\n// ── Favicon upload ──\nfaviconFile.addEventListener('change',function(){\n  const file=this.files[0];\n  if(!file)return;\n  const reader=new FileReader();\n  reader.onload=function(e){\n    faviconDataUrl=e.target.result;\n    faviconClear.style.display='inline';\n    updatePreview();\n  };\n  reader.readAsDataURL(file);\n});\n\nfaviconClear.addEventListener('click',function(){\n  faviconDataUrl=null;\n  faviconFile.value='';\n  faviconClear.style.display='none';\n  updatePreview();\n});\n\n// ── FAQ toggle ──\nfunction toggleFaq(el){\n  el.parentElement.classList.toggle('open');\n}\n\n// ── Live update on all inputs ──\n[titleInput,descInput,urlInput,siteNameInput,pubDateInput,breadcrumbInput].forEach(function(el){\n  el.addEventListener('input',function(){updatePreview();});\n  el.addEventListener('change',function(){updatePreview();});\n});\ntitleInput.addEventListener('input',updateTitleCount);\ndescInput.addEventListener('input',updateDescCount);\nurlInput.addEventListener('input',updateUrlCount);\n\n// ── SEO Score ──\nfunction checkSeoScore(){\n  const panel=document.getElementById('scorePanel');\n  const badge=document.getElementById('scoreBadge');\n  const items=document.getElementById('scoreItems');\n  panel.style.display='block';\n  items.innerHTML='';\n\n  const title=titleInput.value.trim();\n  const desc=descInput.value.trim();\n  const url=urlInput.value.trim();\n  let score=0;\n  const checks=[];\n\n  // Title length\n  if(title.length>=50&&title.length<=60){\n    checks.push({pass:true,text:'Title length is perfect ('+title.length+' chars)'});score+=20;\n  }else if(title.length>=40&&title.length<=65){\n    checks.push({warn:true,text:'Title length is acceptable ('+title.length+' chars) but aim for 50-60'});score+=12;\n  }else if(title.length>0){\n    checks.push({fail:true,text:'Title is '+(title.length<40?'too short':'too long')+' ('+title.length+' chars). Aim for 50-60.'});score+=4;\n  }else{\n    checks.push({fail:true,text:'No title provided. This is the most important SEO element.'});\n  }\n\n  // Title has separator (brand)\n  if(title&&(title.includes('|')||title.includes(' - ')||title.includes(' \\u2014 '))){\n    checks.push({pass:true,text:'Title includes a brand separator'});score+=10;\n  }else if(title){\n    checks.push({warn:true,text:'Consider adding your brand name with a separator (| or \\u2014)'});score+=4;\n  }\n\n  // Title starts with keyword (not generic)\n  if(title&&title.length>0&&/^[A-Z]/.test(title)){\n    checks.push({pass:true,text:'Title starts with a capitalized word (likely keyword)'});score+=5;\n  }\n\n  // Description length\n  if(desc.length>=120&&desc.length<=160){\n    checks.push({pass:true,text:'Description length is perfect ('+desc.length+' chars)'});score+=20;\n  }else if(desc.length>=100&&desc.length<=170){\n    checks.push({warn:true,text:'Description length is acceptable ('+desc.length+' chars) but aim for 120-160'});score+=12;\n  }else if(desc.length>0){\n    checks.push({fail:true,text:'Description is '+(desc.length<100?'too short':'too long')+' ('+desc.length+' chars). Aim for 120-160.'});score+=4;\n  }else{\n    checks.push({fail:true,text:'No description provided. Google may generate one, but it is better to control your snippet.'});\n  }\n\n  // Description has call to action\n  const ctaWords=['learn','discover','find','get','try','start','see','read','check','explore','free','today','now'];\n  const descLower=desc.toLowerCase();\n  const hasCta=ctaWords.some(w=>descLower.includes(w));\n  if(desc&&hasCta){\n    checks.push({pass:true,text:'Description contains action-oriented language'});score+=10;\n  }else if(desc){\n    checks.push({warn:true,text:'Consider adding a call to action (e.g., \"Learn more,\" \"Get started,\" \"Try free\")'});score+=3;\n  }\n\n  // URL checks\n  if(url){\n    const cleanUrl=url.toLowerCase();\n    if(cleanUrl.includes(' ')||cleanUrl.includes('_')){\n      checks.push({warn:true,text:'URL contains spaces or underscores. Use hyphens instead.'});score+=3;\n    }else{\n      checks.push({pass:true,text:'URL uses clean formatting'});score+=10;\n    }\n    if(cleanUrl.length>75){\n      checks.push({warn:true,text:'URL is long ('+url.length+' chars). Shorter URLs tend to perform better.'});score+=3;\n    }else{\n      checks.push({pass:true,text:'URL length is good'});score+=5;\n    }\n    if(/[?&=#]/.test(cleanUrl)){\n      checks.push({warn:true,text:'URL contains query parameters. Clean, readable URLs are preferred.'});score+=2;\n    }\n  }else{\n    checks.push({fail:true,text:'No URL provided.'});\n  }\n\n  // Title and desc not identical\n  if(title&&desc&&title.toLowerCase()===desc.toLowerCase()){\n    checks.push({fail:true,text:'Title and description are identical. They should be different.'});\n  }else if(title&&desc){\n    checks.push({pass:true,text:'Title and description are unique from each other'});score+=5;\n  }\n\n  // Number not too high\n  const maxScore=85;\n  const pct=Math.min(100,Math.round((score/maxScore)*100));\n\n  badge.textContent=pct+'/100';\n  if(pct>=80){badge.className='score-badge good';}\n  else if(pct>=50){badge.className='score-badge ok';}\n  else{badge.className='score-badge bad';}\n\n  checks.forEach(function(c){\n    const div=document.createElement('div');\n    div.className='score-item';\n    let icon,cls;\n    if(c.pass){icon='\\u2713';cls='score-pass';}\n    else if(c.warn){icon='\\u26A0';cls='score-warn';}\n    else{icon='\\u2717';cls='score-fail';}\n    div.innerHTML='<span class=\"score-icon '+cls+'\">'+icon+'</span><span>'+c.text+'</span>';\n    items.appendChild(div);\n  });\n\n  panel.scrollIntoView({behavior:'smooth',block:'nearest'});\n}\n\n// ── Copy meta tags ──\nfunction copyMetaTags(){\n  const title=titleInput.value.trim();\n  const desc=descInput.value.trim();\n  const url=urlInput.value.trim();\n\n  if(!title&&!desc){\n    showToast('Enter a title or description first');\n    return;\n  }\n\n  let lines=[];\n  if(title) lines.push('<title>'+esc(title)+'</title>');\n  if(desc) lines.push('<meta name=\"description\" content=\"'+esc(desc)+'\">');\n  if(url) lines.push('<link rel=\"canonical\" href=\"'+esc(url)+'\">');\n  if(title){\n    lines.push('<meta property=\"og:title\" content=\"'+esc(title)+'\">');\n  }\n  if(desc){\n    lines.push('<meta property=\"og:description\" content=\"'+esc(desc)+'\">');\n  }\n  if(url){\n    lines.push('<meta property=\"og:url\" content=\"'+esc(url)+'\">');\n  }\n  lines.push('<meta property=\"og:type\" content=\"website\">');\n  lines.push('<meta name=\"twitter:card\" content=\"summary\">');\n  if(title) lines.push('<meta name=\"twitter:title\" content=\"'+esc(title)+'\">');\n  if(desc) lines.push('<meta name=\"twitter:description\" content=\"'+esc(desc)+'\">');\n\n  const code=lines.join('\\n');\n\n  const codePanel=document.getElementById('codePanel');\n  document.getElementById('codeOutput').textContent=code;\n  codePanel.style.display='block';\n\n  navigator.clipboard.writeText(code).then(function(){showToast('Meta tags copied to clipboard!');}).catch(function(){\n    const ta=document.createElement('textarea');\n    ta.value=code;document.body.appendChild(ta);ta.select();document.execCommand('copy');document.body.removeChild(ta);\n    showToast('Meta tags copied to clipboard!');\n  });\n\n  codePanel.scrollIntoView({behavior:'smooth',block:'nearest'});\n}\n\nfunction copyCode(){\n  const code=document.getElementById('codeOutput').textContent;\n  navigator.clipboard.writeText(code).then(function(){showToast('Copied!');}).catch(function(){\n    const ta=document.createElement('textarea');\n    ta.value=code;document.body.appendChild(ta);ta.select();document.execCommand('copy');document.body.removeChild(ta);\n    showToast('Copied!');\n  });\n}\n\n// ── Reset ──\nfunction resetForm(){\n  titleInput.value='';descInput.value='';urlInput.value='';\n  siteNameInput.value='';pubDateInput.value='';breadcrumbInput.value='';\n  faviconDataUrl=null;faviconFile.value='';faviconClear.style.display='none';\n  document.getElementById('scorePanel').style.display='none';\n  document.getElementById('codePanel').style.display='none';\n  updateTitleCount();updateDescCount();updateUrlCount();updatePreview();\n}\n\n// ── Initial render ──\nupdatePreview();\n\n// ── Ads ──\nconst ads=[\"Custom Print on Demand Apparel \\u2014 Free Storefront for Your Business\",\"Sell Custom Apparel \\u2014 We Handle Printing & Free Shipping\",\"Launch Your Own Clothing Brand \\u2014 No Inventory, No Risk\"];let adIdx=Math.floor(Math.random()*ads.length);\nfunction rotateAds(){const el=document.getElementById('ad');if(el)el.textContent=ads[adIdx];const el2=document.getElementById('ad-bottom');if(el2)el2.textContent=ads[(adIdx+1)%ads.length];adIdx=(adIdx+1)%ads.length;}rotateAds();setInterval(rotateAds,8000);\n";
