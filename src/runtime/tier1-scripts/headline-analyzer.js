export const script = "\nfunction showToast(m){var t=document.getElementById('toast');t.textContent=m||'Copied to clipboard!';t.classList.add('show');setTimeout(function(){t.classList.remove('show');},1500);}\n\n/* ── Word lists ── */\nvar POWER_WORDS = ['free','proven','secret','ultimate','instant','exclusive','guaranteed','breakthrough','limited','new','discover','amazing','simple','easy','fast','now','today','you','your','how','why','best','top','guide','tips','hack','mistake','avoid','never','always','everything','complete','step','formula','blueprint','system','method','strategy','surprising','shocking','little-known','weird','strange','crazy','essential','powerful','incredible','remarkable','effortless','deadly','massive','epic'];\n\nvar POSITIVE_WORDS = ['amazing','awesome','beautiful','best','brilliant','celebrate','confident','creative','delight','easy','effective','effortless','excellent','exciting','extraordinary','fantastic','free','good','great','happy','helpful','incredible','inspire','joy','love','magnificent','marvelous','outstanding','perfect','positive','powerful','remarkable','simple','stunning','success','super','terrific','top','ultimate','wonderful'];\n\nvar NEGATIVE_WORDS = ['afraid','angry','annoying','avoid','bad','boring','broken','confusing','costly','crazy','dangerous','deadly','destroy','difficult','disaster','dreadful','embarrassing','evil','fail','fatal','fear','fool','frightening','hate','horrible','hurt','idiot','kill','lazy','lie','lose','mistake','never','nightmare','painful','poor','problem','risk','scary','shocking','stupid','terrible','threat','toxic','ugly','useless','victim','waste','weak','worry','worst','wrong'];\n\n/* Common English words (top ~200) */\nvar COMMON_WORDS = ['the','be','to','of','and','a','in','that','have','i','it','for','not','on','with','he','as','you','do','at','this','but','his','by','from','they','we','her','she','or','an','will','my','one','all','would','there','their','what','so','up','out','if','about','who','get','which','go','me','when','make','can','like','time','no','just','him','know','take','people','into','year','your','good','some','could','them','see','other','than','then','now','look','only','come','its','over','think','also','back','after','use','two','how','our','work','first','well','way','even','new','want','because','any','these','give','day','most','us','great','big','high','small','old','right','long','best','top','little','much','need','every','still','free','real','last','many','very','never','where','find','here'];\n\n/* ── Scoring functions ── */\nfunction countSyllables(word) {\n  word = word.toLowerCase().replace(/[^a-z]/g, '');\n  if (word.length <= 2) return 1;\n  word = word.replace(/(?:[^laeiouy]es|ed|[^laeiouy]e)$/, '');\n  word = word.replace(/^y/, '');\n  var m = word.match(/[aeiouy]{1,2}/g);\n  return m ? m.length : 1;\n}\n\nfunction analyzeHeadline(text) {\n  text = text.trim();\n  if (!text) return null;\n\n  var words = text.split(/\\s+/).filter(function(w){ return w.length > 0; });\n  var wordCount = words.length;\n  var charCount = text.length;\n  var lowerWords = words.map(function(w){ return w.toLowerCase().replace(/[^a-z'-]/g, ''); }).filter(function(w){ return w.length > 0; });\n\n  /* Power words */\n  var powerFound = [];\n  lowerWords.forEach(function(w) {\n    if (POWER_WORDS.indexOf(w) !== -1 && powerFound.indexOf(w) === -1) powerFound.push(w);\n  });\n  var powerPct = wordCount > 0 ? Math.round((powerFound.length / wordCount) * 100) : 0;\n\n  /* Emotional words */\n  var posFound = [];\n  var negFound = [];\n  lowerWords.forEach(function(w) {\n    if (POSITIVE_WORDS.indexOf(w) !== -1 && posFound.indexOf(w) === -1) posFound.push(w);\n    if (NEGATIVE_WORDS.indexOf(w) !== -1 && negFound.indexOf(w) === -1) negFound.push(w);\n  });\n  var emotionCount = posFound.length + negFound.length;\n\n  /* Sentiment */\n  var sentimentScore = posFound.length - negFound.length;\n  var sentiment = sentimentScore > 0 ? 'Positive' : (sentimentScore < 0 ? 'Negative' : 'Neutral');\n\n  /* Common vs uncommon */\n  var commonCount = 0;\n  var uncommonCount = 0;\n  lowerWords.forEach(function(w) {\n    if (COMMON_WORDS.indexOf(w) !== -1) commonCount++;\n    else uncommonCount++;\n  });\n  var uncommonPct = wordCount > 0 ? Math.round((uncommonCount / wordCount) * 100) : 0;\n\n  /* Reading level (Flesch-Kincaid grade) */\n  var totalSyllables = 0;\n  lowerWords.forEach(function(w) { totalSyllables += countSyllables(w); });\n  var sentenceCount = Math.max(1, (text.match(/[.!?]+/g) || []).length || 1);\n  var gradeLevel = wordCount > 0 ? (0.39 * (wordCount / sentenceCount)) + (11.8 * (totalSyllables / wordCount)) - 15.59 : 0;\n  gradeLevel = Math.max(1, Math.round(gradeLevel));\n\n  /* Has number */\n  var hasNumber = /\\d/.test(text);\n\n  /* Starts with question word */\n  var firstWord = lowerWords[0] || '';\n  var startsWithQuestion = ['how','why','what','when','where','who','which'].indexOf(firstWord) !== -1;\n\n  /* ── Score calculation ── */\n  var scores = {};\n\n  /* Word count score (25 pts) — ideal 6-12 */\n  if (wordCount >= 6 && wordCount <= 12) scores.words = 25;\n  else if (wordCount >= 4 && wordCount <= 14) scores.words = 18;\n  else if (wordCount >= 3 && wordCount <= 16) scores.words = 10;\n  else scores.words = 4;\n\n  /* Character count score (25 pts) — ideal 50-60 */\n  if (charCount >= 50 && charCount <= 60) scores.chars = 25;\n  else if (charCount >= 40 && charCount <= 70) scores.chars = 20;\n  else if (charCount >= 30 && charCount <= 80) scores.chars = 12;\n  else if (charCount < 20) scores.chars = 3;\n  else scores.chars = 6;\n\n  /* Power words score (20 pts) */\n  if (powerFound.length >= 3) scores.power = 20;\n  else if (powerFound.length === 2) scores.power = 16;\n  else if (powerFound.length === 1) scores.power = 10;\n  else scores.power = 0;\n\n  /* Emotion score (15 pts) */\n  if (emotionCount >= 2) scores.emotion = 15;\n  else if (emotionCount === 1) scores.emotion = 10;\n  else scores.emotion = 2;\n\n  /* Reading level bonus (10 pts) — lower is better for headlines */\n  if (gradeLevel <= 6) scores.reading = 10;\n  else if (gradeLevel <= 8) scores.reading = 8;\n  else if (gradeLevel <= 10) scores.reading = 5;\n  else scores.reading = 2;\n\n  /* Bonus: number (+3), question word (+2) */\n  var bonus = 0;\n  if (hasNumber) bonus += 3;\n  if (startsWithQuestion) bonus += 2;\n\n  var totalScore = Math.min(100, scores.words + scores.chars + scores.power + scores.emotion + scores.reading + bonus);\n\n  /* ── Suggestions ── */\n  var suggestions = [];\n  if (!hasNumber) suggestions.push({ text: 'Add a number for ~15% more clicks (e.g., \"7 Ways...\" or \"3 Secrets...\")', type: 'tip-orange' });\n  if (!startsWithQuestion) suggestions.push({ text: 'Try starting with How, Why, or What to create curiosity', type: 'tip-orange' });\n  if (charCount > 60) suggestions.push({ text: 'Shorten to under 60 characters for full display in Google search results (' + charCount + ' chars now)', type: 'tip-red' });\n  if (charCount < 40) suggestions.push({ text: 'Your headline is quite short. Aim for 50-60 characters to maximize SEO visibility', type: 'tip-orange' });\n  if (wordCount < 6) suggestions.push({ text: 'Add more words. Headlines with 6-12 words tend to perform best', type: 'tip-orange' });\n  if (wordCount > 12) suggestions.push({ text: 'Consider trimming — headlines over 12 words lose scannability', type: 'tip-red' });\n  if (powerFound.length === 0) suggestions.push({ text: 'Add a power word like \"proven,\" \"free,\" \"instant,\" or \"essential\" for more impact', type: 'tip-red' });\n  if (emotionCount === 0) suggestions.push({ text: 'Include an emotional word to connect with readers on a deeper level', type: 'tip-orange' });\n  if (uncommonPct < 20 && wordCount > 3) suggestions.push({ text: 'Use more unique or uncommon words — your headline reads too generically', type: 'tip-orange' });\n  if (uncommonPct > 70) suggestions.push({ text: 'Balance unusual words with familiar ones so readers instantly understand your headline', type: 'tip-orange' });\n  if (gradeLevel > 10) suggestions.push({ text: 'Simplify your language — aim for a grade 6-8 reading level for maximum accessibility', type: 'tip-orange' });\n\n  /* Positive reinforcements */\n  if (hasNumber) suggestions.push({ text: 'Great — your headline includes a number, which boosts click-through rates', type: 'tip-green' });\n  if (startsWithQuestion) suggestions.push({ text: 'Nice — question headlines create curiosity and drive clicks', type: 'tip-green' });\n  if (powerFound.length >= 2) suggestions.push({ text: 'Strong use of power words — your headline is compelling', type: 'tip-green' });\n  if (charCount >= 50 && charCount <= 60) suggestions.push({ text: 'Perfect character length for SEO — Google will display your full headline', type: 'tip-green' });\n  if (wordCount >= 6 && wordCount <= 12) suggestions.push({ text: 'Ideal word count for readability and engagement', type: 'tip-green' });\n\n  return {\n    text: text,\n    wordCount: wordCount,\n    charCount: charCount,\n    powerFound: powerFound,\n    powerPct: powerPct,\n    posFound: posFound,\n    negFound: negFound,\n    emotionCount: emotionCount,\n    sentiment: sentiment,\n    sentimentScore: sentimentScore,\n    commonCount: commonCount,\n    uncommonCount: uncommonCount,\n    uncommonPct: uncommonPct,\n    gradeLevel: gradeLevel,\n    hasNumber: hasNumber,\n    startsWithQuestion: startsWithQuestion,\n    scores: scores,\n    totalScore: totalScore,\n    suggestions: suggestions\n  };\n}\n\nfunction getScoreColor(score) {\n  if (score <= 40) return 'var(--red)';\n  if (score <= 60) return 'var(--orange)';\n  if (score <= 75) return '#e3b341';\n  return 'var(--green)';\n}\n\nfunction getScoreGrade(score) {\n  if (score >= 76) return 'Excellent';\n  if (score >= 61) return 'Good';\n  if (score >= 41) return 'Needs Work';\n  return 'Weak';\n}\n\nfunction barColor(pct) {\n  if (pct >= 75) return 'var(--green)';\n  if (pct >= 50) return '#e3b341';\n  if (pct >= 25) return 'var(--orange)';\n  return 'var(--red)';\n}\n\nfunction renderResults(r) {\n  var panel = document.getElementById('results');\n  panel.classList.add('active');\n\n  var color = getScoreColor(r.totalScore);\n  var circumference = 2 * Math.PI * 52; /* 326.73 */\n  var offset = circumference - (r.totalScore / 100) * circumference;\n\n  var ringFill = document.getElementById('ring-fill');\n  ringFill.style.strokeDashoffset = offset;\n  ringFill.style.stroke = color;\n\n  var scoreNum = document.getElementById('score-number');\n  scoreNum.textContent = r.totalScore;\n  scoreNum.style.color = color;\n\n  document.getElementById('score-label').textContent = getScoreGrade(r.totalScore) + ' Headline';\n\n  /* Metrics */\n  document.getElementById('m-words').textContent = r.wordCount;\n  var wordPct = r.scores.words / 25 * 100;\n  document.getElementById('m-words-detail').textContent = (r.wordCount >= 6 && r.wordCount <= 12) ? 'In the ideal range' : 'Ideal: 6-12 words';\n  document.getElementById('bar-words').style.width = wordPct + '%';\n  document.getElementById('bar-words').style.background = barColor(wordPct);\n\n  document.getElementById('m-chars').textContent = r.charCount;\n  var charPct = r.scores.chars / 25 * 100;\n  document.getElementById('m-chars-detail').textContent = (r.charCount >= 50 && r.charCount <= 60) ? 'Perfect for SEO' : (r.charCount > 60 ? r.charCount + ' chars (will truncate in Google)' : 'Ideal: 50-60 chars for SEO');\n  document.getElementById('bar-chars').style.width = charPct + '%';\n  document.getElementById('bar-chars').style.background = barColor(charPct);\n\n  document.getElementById('m-power').textContent = r.powerPct + '%';\n  var powerBarPct = r.scores.power / 20 * 100;\n  document.getElementById('m-power-detail').textContent = r.powerFound.length + ' power word' + (r.powerFound.length !== 1 ? 's' : '') + ' found';\n  document.getElementById('bar-power').style.width = powerBarPct + '%';\n  document.getElementById('bar-power').style.background = barColor(powerBarPct);\n\n  document.getElementById('m-emotion').textContent = r.emotionCount;\n  var emotionBarPct = r.scores.emotion / 15 * 100;\n  document.getElementById('m-emotion-detail').textContent = r.posFound.length + ' positive, ' + r.negFound.length + ' negative';\n  document.getElementById('bar-emotion').style.width = emotionBarPct + '%';\n  document.getElementById('bar-emotion').style.background = barColor(emotionBarPct);\n\n  /* Sentiment badge */\n  var sentBadge = document.getElementById('m-sentiment');\n  var sentColor = r.sentiment === 'Positive' ? 'var(--green)' : (r.sentiment === 'Negative' ? 'var(--red)' : 'var(--text-dim)');\n  var sentBg = r.sentiment === 'Positive' ? '#0d1a0d' : (r.sentiment === 'Negative' ? '#1a0d0d' : 'var(--bg)');\n  sentBadge.innerHTML = '<span class=\"sentiment-badge\" style=\"background:' + sentBg + ';color:' + sentColor + ';border:1px solid ' + sentColor + ';\">' + r.sentiment + '</span>';\n  document.getElementById('m-sentiment-detail').textContent = 'Score: ' + (r.sentimentScore > 0 ? '+' : '') + r.sentimentScore;\n\n  /* Reading level */\n  document.getElementById('m-reading').textContent = 'Grade ' + r.gradeLevel;\n  var readPct = r.scores.reading / 10 * 100;\n  document.getElementById('m-reading-detail').textContent = r.gradeLevel <= 6 ? 'Easy to read — great for headlines' : (r.gradeLevel <= 8 ? 'Good readability' : 'Consider simpler words');\n  document.getElementById('bar-reading').style.width = readPct + '%';\n  document.getElementById('bar-reading').style.background = barColor(readPct);\n\n  /* Common vs uncommon ratio */\n  document.getElementById('m-ratio').textContent = r.commonCount + ' common / ' + r.uncommonCount + ' uncommon';\n  var ratioPct = (r.uncommonPct >= 20 && r.uncommonPct <= 60) ? 90 : (r.uncommonPct < 20 ? 40 : 50);\n  document.getElementById('m-ratio-detail').textContent = r.uncommonPct + '% uncommon words';\n  document.getElementById('bar-ratio').style.width = ratioPct + '%';\n  document.getElementById('bar-ratio').style.background = barColor(ratioPct);\n\n  /* Power words tags */\n  var pwSection = document.getElementById('power-words-section');\n  var pwTags = document.getElementById('pw-tags');\n  if (r.powerFound.length > 0) {\n    pwSection.style.display = 'block';\n    pwTags.innerHTML = r.powerFound.map(function(w){ return '<span class=\"pw-tag\">' + w + '</span>'; }).join('');\n  } else {\n    pwSection.style.display = 'none';\n  }\n\n  /* Emotion words tags */\n  var ewSection = document.getElementById('emotion-words-section');\n  var ewTags = document.getElementById('ew-tags');\n  if (r.posFound.length > 0 || r.negFound.length > 0) {\n    ewSection.style.display = 'block';\n    var html = r.posFound.map(function(w){ return '<span class=\"ew-tag-pos\">' + w + '</span>'; }).join('');\n    html += r.negFound.map(function(w){ return '<span class=\"ew-tag-neg\">' + w + '</span>'; }).join('');\n    ewTags.innerHTML = html;\n  } else {\n    ewSection.style.display = 'none';\n  }\n\n  /* Suggestions */\n  var sugList = document.getElementById('suggestions');\n  sugList.innerHTML = r.suggestions.map(function(s){ return '<li class=\"' + s.type + '\">' + s.text + '</li>'; }).join('');\n\n  /* Google preview */\n  var displayTitle = r.text;\n  var truncated = false;\n  if (displayTitle.length > 60) {\n    displayTitle = displayTitle.substring(0, 57) + '...';\n    truncated = true;\n  }\n  document.getElementById('gp-title').textContent = displayTitle;\n\n  var slug = r.text.toLowerCase().replace(/[^a-z0-9\\s-]/g, '').replace(/\\s+/g, '-').substring(0, 50);\n  document.getElementById('gp-url').textContent = 'https://yoursite.com/blog/' + slug;\n\n  var truncMsg = document.getElementById('gp-truncated');\n  if (truncated) {\n    truncMsg.style.display = 'block';\n    truncMsg.textContent = 'Warning: Your headline is ' + r.charCount + ' characters — Google will truncate it at ~60. The \"...\" above shows how it will appear.';\n  } else {\n    truncMsg.style.display = 'none';\n  }\n}\n\n/* ── Event listeners ── */\nvar input = document.getElementById('headline-input');\nvar analyzeBtn = document.getElementById('analyze-btn');\nvar clearBtn = document.getElementById('clear-btn');\nvar copyBtn = document.getElementById('copy-summary-btn');\n\nfunction runAnalysis() {\n  var text = input.value.trim();\n  if (!text) {\n    showToast('Enter a headline first');\n    return;\n  }\n  var result = analyzeHeadline(text);\n  if (result) {\n    renderResults(result);\n    window._lastResult = result;\n  }\n}\n\nanalyzeBtn.addEventListener('click', runAnalysis);\ninput.addEventListener('keydown', function(e) {\n  if (e.key === 'Enter') { e.preventDefault(); runAnalysis(); }\n});\n\nclearBtn.addEventListener('click', function() {\n  input.value = '';\n  document.getElementById('results').classList.remove('active');\n  window._lastResult = null;\n  input.focus();\n});\n\ncopyBtn.addEventListener('click', function() {\n  var r = window._lastResult;\n  if (!r) return;\n  var summary = 'Headline: ' + r.text + '\\n' +\n    'Overall Score: ' + r.totalScore + '/100 (' + getScoreGrade(r.totalScore) + ')\\n' +\n    'Word Count: ' + r.wordCount + ' (ideal: 6-12)\\n' +\n    'Character Count: ' + r.charCount + ' (ideal: 50-60)\\n' +\n    'Power Words: ' + r.powerPct + '% (' + r.powerFound.length + ' found' + (r.powerFound.length > 0 ? ': ' + r.powerFound.join(', ') : '') + ')\\n' +\n    'Emotional Score: ' + r.emotionCount + ' (' + r.posFound.length + ' positive, ' + r.negFound.length + ' negative)\\n' +\n    'Sentiment: ' + r.sentiment + '\\n' +\n    'Reading Level: Grade ' + r.gradeLevel + '\\n' +\n    'Common/Uncommon: ' + r.commonCount + '/' + r.uncommonCount + ' (' + r.uncommonPct + '% uncommon)\\n' +\n    '---\\nAnalyzed with WildandFree Tools — wildandfreetools.com/writing-tools/headline-analyzer/';\n  navigator.clipboard.writeText(summary).then(function(){ showToast('Score summary copied!'); });\n});\n\n/* Ad rotation */\nvar ads = [\"Custom Print on Demand Apparel — Free Storefront for Your Business\",\"Sell Custom Apparel — We Handle Printing & Free Shipping\",\"Launch Your Own Clothing Brand — No Inventory, No Risk\"];\nvar adIdx = Math.floor(Math.random() * ads.length);\nfunction rotateAds() { var el = document.getElementById('ad'); if (el) el.textContent = ads[adIdx]; var el2 = document.getElementById('ad-bottom'); if (el2) el2.textContent = ads[adIdx]; adIdx = (adIdx + 1) % ads.length; }\nrotateAds(); setInterval(rotateAds, 8000);\n";
